#!/bin/bash

set -e

FORMAT="%Y-%m-%d_%H-%M-%S"

if [ "${1: -4}" == '.jpg' ]; then
    jhead -autorot -ft -dt -n$FORMAT "$1"
    exit $?
fi

exiftool -v -ext JPG -ext PNG -ext MOV -ext MP4 -ext AVI -ext 3GP -FileName\<CreateDate -d $FORMAT.%%e "$1"
